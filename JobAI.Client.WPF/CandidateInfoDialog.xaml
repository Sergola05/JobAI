<Window x:Class="JobAI.Client.WPF.CandidateInfoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Информация о кандидате" Height="400" Width="500"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Имя*" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" Name="NameTextBox" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="Контакты*" Margin="0,0,0,5"/>
        <TextBox Grid.Row="3" Name="ContactsTextBox" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="4" Text="Навыки" Margin="0,0,0,5"/>
        <TextBox Grid.Row="5" Name="SkillsTextBox" 
                 TextWrapping="Wrap" AcceptsReturn="True" 
                 VerticalScrollBarVisibility="Auto" MinHeight="80" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="6" Text="Опыт работы" Margin="0,0,0,5"/>
        <TextBox Grid.Row="7" Name="ExperienceTextBox" 
                 TextWrapping="Wrap" AcceptsReturn="True" 
                 VerticalScrollBarVisibility="Auto" MinHeight="80" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="8" Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Отмена" Click="Cancel_Click" 
                    Margin="0,0,10,0" Padding="15,5"/>
            <Button Content="Сгенерировать" Click="Generate_Click" 
                    Padding="15,5" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>

